export default function MarketingPage() {
  return (
    <main style={{ display: 'grid', gap: '1rem' }}>
      <h2>Landing Page</h2>
      <p>Segment `(marketing)` dapat memuat halaman publik tanpa guard.</p>
    </main>
  );
}
