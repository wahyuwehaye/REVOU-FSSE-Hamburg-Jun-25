# 🏗️ Blog API - Architecture & Flow Documentation

## 📋 Table of Contents

1. [Project Overview](#project-overview)
2. [Architecture Diagram](#architecture-diagram)
3. [Database Schema & Relationships](#database-schema--relationships)
4. [Authentication Flow](#authentication-flow)
5. [Authorization Flow (RBAC)](#authorization-flow-rbac)
6. [API Request Flow](#api-request-flow)
7. [CRUD Operations Flow](#crud-operations-flow)
8. [Security Layers](#security-layers)
9. [Complete User Journey](#complete-user-journey)
10. [Deployment Flow](#deployment-flow)

---

## 🎯 Project Overview

Blog API adalah aplikasi backend yang dibangun dengan **NestJS** dan **Prisma ORM**, mendemonstrasikan konsep-konsep advanced dalam pengembangan API:

- ✅ **Database Relations** (1:1, 1:N, M:N)
- ✅ **JWT Authentication** dengan Refresh Tokens
- ✅ **Role-Based Access Control (RBAC)**
- ✅ **Security Best Practices**
- ✅ **RESTful API Design**

### Tech Stack

```
┌─────────────────────────────────────────────┐
│           Application Layer                  │
│  ┌─────────────────────────────────────┐   │
│  │         NestJS Framework            │   │
│  │  (Controllers, Services, Guards)    │   │
│  └─────────────────────────────────────┘   │
├─────────────────────────────────────────────┤
│           Data Access Layer                  │
│  ┌─────────────────────────────────────┐   │
│  │         Prisma ORM                  │   │
│  │  (Type-safe Database Client)        │   │
│  └─────────────────────────────────────┘   │
├─────────────────────────────────────────────┤
│           Database Layer                     │
│  ┌─────────────────────────────────────┐   │
│  │          MySQL 8.0                  │   │
│  │  (Relational Database)              │   │
│  └─────────────────────────────────────┘   │
└─────────────────────────────────────────────┘
```

---

## 🏛️ Architecture Diagram

### High-Level Architecture

```
┌──────────────┐
│   Client     │
│  (Postman/   │
│   Browser)   │
└──────┬───────┘
       │ HTTP Request
       ▼
┌─────────────────────────────────────────────────────┐
│              NestJS Application                      │
│                                                      │
│  ┌────────────────────────────────────────────┐   │
│  │         Middleware Layer                    │   │
│  │  • Helmet (Security Headers)               │   │
│  │  • CORS (Cross-Origin)                     │   │
│  │  • Throttler (Rate Limiting)               │   │
│  └─────────────┬──────────────────────────────┘   │
│                ▼                                    │
│  ┌────────────────────────────────────────────┐   │
│  │         Guard Layer                         │   │
│  │  • JwtAuthGuard (Authentication)           │   │
│  │  • RolesGuard (Authorization)              │   │
│  └─────────────┬──────────────────────────────┘   │
│                ▼                                    │
│  ┌────────────────────────────────────────────┐   │
│  │         Controller Layer                    │   │
│  │  • AuthController                          │   │
│  │  • UsersController                         │   │
│  │  • PostsController                         │   │
│  │  • CategoriesController                    │   │
│  │  • CommentsController                      │   │
│  │  • TagsController                          │   │
│  │  • ProfilesController                      │   │
│  └─────────────┬──────────────────────────────┘   │
│                ▼                                    │
│  ┌────────────────────────────────────────────┐   │
│  │         Validation Layer                    │   │
│  │  • ValidationPipe (DTO Validation)         │   │
│  │  • class-validator                         │   │
│  └─────────────┬──────────────────────────────┘   │
│                ▼                                    │
│  ┌────────────────────────────────────────────┐   │
│  │         Service Layer                       │   │
│  │  • Business Logic                          │   │
│  │  • Data Transformation                     │   │
│  │  • Ownership Validation                    │   │
│  └─────────────┬──────────────────────────────┘   │
│                ▼                                    │
│  ┌────────────────────────────────────────────┐   │
│  │         Prisma Service                      │   │
│  │  • Database Queries                        │   │
│  │  • Transaction Management                  │   │
│  └─────────────┬──────────────────────────────┘   │
└────────────────┼────────────────────────────────────┘
                 ▼
         ┌──────────────┐
         │    MySQL     │
         │   Database   │
         └──────────────┘
```

---

## 🗄️ Database Schema & Relationships

### Entity Relationship Diagram

```
┌─────────────────┐
│      User       │
│─────────────────│
│ id: Int         │◄─┐
│ email: String   │  │
│ name: String    │  │ 1:1
│ password: String│  │
│ role: Enum      │  │
│ refreshToken    │  │
└────────┬────────┘  │
         │           │
         │ 1:N       │
         │           │
         ▼           │
┌─────────────────┐  │
│     Profile     │  │
│─────────────────│  │
│ id: Int         │  │
│ userId: Int     │──┘ (unique)
│ bio: String     │
│ avatar: String  │
│ website: String │
│ location: String│
└─────────────────┘

         │
         │ 1:N (Posts)
         │
         ▼
┌─────────────────┐         ┌─────────────────┐
│      Post       │  M:1    │    Category     │
│─────────────────│◄────────│─────────────────│
│ id: Int         │         │ id: Int         │
│ title: String   │         │ name: String    │
│ slug: String    │         │ slug: String    │
│ content: Text   │         │ description     │
│ published: Bool │         └─────────────────┘
│ authorId: Int   │
│ categoryId: Int │
└────────┬────────┘
         │
         │ 1:N
         │
         ▼
┌─────────────────┐
│    Comment      │
│─────────────────│
│ id: Int         │
│ content: Text   │
│ postId: Int     │
│ authorId: Int   │
└─────────────────┘

┌─────────────────┐         ┌─────────────────┐         ┌─────────────────┐
│      Post       │   M:N   │    PostTag      │   M:N   │      Tag        │
│─────────────────│◄────────│─────────────────│────────►│─────────────────│
│ id: Int         │         │ id: Int         │         │ id: Int         │
│ title: String   │         │ postId: Int     │         │ name: String    │
└─────────────────┘         │ tagId: Int      │         │ slug: String    │
                            └─────────────────┘         └─────────────────┘
                            (Explicit Join Table)
```

### Relationship Types Explained

#### 1️⃣ **One-to-One (1:1)** - User ↔ Profile
```prisma
model User {
  id       Int      @id @default(autoincrement())
  email    String   @unique
  profile  Profile? // One user has one profile
}

model Profile {
  id     Int    @id @default(autoincrement())
  userId Int    @unique // Ensures 1:1 relationship
  user   User   @relation(fields: [userId], references: [id])
}
```

**Use Case**: Setiap user hanya punya 1 profile, dan profile hanya milik 1 user.

#### 2️⃣ **One-to-Many (1:N)** - User → Posts, Post → Comments
```prisma
model User {
  id    Int    @id @default(autoincrement())
  posts Post[] // One user has many posts
}

model Post {
  id       Int       @id @default(autoincrement())
  authorId Int
  author   User      @relation(fields: [authorId], references: [id])
  comments Comment[] // One post has many comments
}
```

**Use Case**: Satu user bisa punya banyak posts, satu post bisa punya banyak comments.

#### 3️⃣ **Many-to-Many (M:N)** - Posts ↔ Tags
```prisma
model Post {
  id   Int       @id @default(autoincrement())
  tags PostTag[] // Many-to-many via join table
}

model Tag {
  id    Int       @id @default(autoincrement())
  posts PostTag[] // Many-to-many via join table
}

model PostTag {
  id     Int  @id @default(autoincrement())
  postId Int
  tagId  Int
  post   Post @relation(fields: [postId], references: [id])
  tag    Tag  @relation(fields: [tagId], references: [id])
  
  @@unique([postId, tagId]) // Prevent duplicate entries
}
```

**Use Case**: Satu post bisa punya banyak tags, satu tag bisa dipakai di banyak posts.

---

## 🔐 Authentication Flow

### Complete Authentication Journey

```
┌──────────────┐
│   Client     │
└──────┬───────┘
       │
       │ 1. POST /auth/register
       │    { email, password, name }
       ▼
┌─────────────────────────────────┐
│    AuthController.register()    │
└──────────┬──────────────────────┘
           │
           ▼
┌─────────────────────────────────┐
│    AuthService.register()       │
│  • Hash password (bcrypt)       │
│  • Create user + profile        │
│  • Generate JWT tokens          │
└──────────┬──────────────────────┘
           │
           ▼
┌─────────────────────────────────┐
│    Generate Tokens              │
│  • Access Token (15 min)        │
│  • Refresh Token (7 days)       │
└──────────┬──────────────────────┘
           │
           ▼
┌─────────────────────────────────┐
│    Store Refresh Token          │
│  • Hash refresh token           │
│  • Save to database             │
└──────────┬──────────────────────┘
           │
           │ Response:
           │ { user, accessToken, refreshToken }
           ▼
┌──────────────┐
│   Client     │
│  (Store      │
│   tokens)    │
└──────────────┘
```

### Login Flow

```
┌──────────────┐
│   Client     │
└──────┬───────┘
       │
       │ 2. POST /auth/login
       │    { email, password }
       ▼
┌─────────────────────────────────┐
│    AuthController.login()       │
└──────────┬──────────────────────┘
           │
           ▼
┌─────────────────────────────────┐
│    AuthService.login()          │
│  1. Find user by email          │
│  2. Verify password (bcrypt)    │
│  3. Generate tokens             │
│  4. Update refresh token        │
└──────────┬──────────────────────┘
           │
           │ Response:
           │ { user, accessToken, refreshToken }
           ▼
┌──────────────┐
│   Client     │
│  (Store      │
│   tokens)    │
└──────────────┘
```

### Protected Route Access

```
┌──────────────┐
│   Client     │
└──────┬───────┘
       │
       │ 3. GET /auth/profile
       │    Headers: Authorization: Bearer <accessToken>
       ▼
┌─────────────────────────────────┐
│    Middleware Layer             │
│  • Helmet                       │
│  • CORS                         │
│  • Rate Limiting                │
└──────────┬──────────────────────┘
           │
           ▼
┌─────────────────────────────────┐
│    JwtAuthGuard                 │
│  1. Extract token from header   │
│  2. Verify JWT signature        │
│  3. Check expiration            │
│  4. Decode payload              │
└──────────┬──────────────────────┘
           │
           ├─── ❌ Invalid/Expired
           │    → 401 Unauthorized
           │
           └─── ✅ Valid
                │
                ▼
┌─────────────────────────────────┐
│    JwtStrategy.validate()       │
│  • Extract user info            │
│  • Return: { sub, email, role } │
└──────────┬──────────────────────┘
           │
           ▼
┌─────────────────────────────────┐
│    Attach to Request            │
│  req.user = payload             │
└──────────┬──────────────────────┘
           │
           ▼
┌─────────────────────────────────┐
│    AuthController.getProfile()  │
│  • Access user from @CurrentUser│
│  • Fetch profile data           │
└──────────┬──────────────────────┘
           │
           │ Response: { user, profile }
           ▼
┌──────────────┐
│   Client     │
└──────────────┘
```

### Token Refresh Flow

```
┌──────────────┐
│   Client     │
│ (Access token│
│  expired)    │
└──────┬───────┘
       │
       │ 4. POST /auth/refresh
       │    { refreshToken }
       ▼
┌─────────────────────────────────┐
│    AuthService.refreshTokens()  │
│  1. Verify refresh token        │
│  2. Find user                   │
│  3. Compare hashed tokens       │
│  4. Generate new tokens         │
└──────────┬──────────────────────┘
           │
           ├─── ❌ Invalid
           │    → 401 Unauthorized
           │
           └─── ✅ Valid
                │
                │ Response: { accessToken, refreshToken }
                ▼
┌──────────────┐
│   Client     │
│  (Update     │
│   tokens)    │
└──────────────┘
```

### JWT Token Structure

```
┌─────────────────────────────────────────────┐
│              JWT Token Structure             │
├─────────────────────────────────────────────┤
│                                             │
│  Header (Algorithm & Type)                  │
│  {                                          │
│    "alg": "HS256",                          │
│    "typ": "JWT"                             │
│  }                                          │
│                                             │
├─────────────────────────────────────────────┤
│                                             │
│  Payload (Claims)                           │
│  {                                          │
│    "sub": 2,              // User ID        │
│    "email": "john@blog.com",                │
│    "role": "AUTHOR",                        │
│    "iat": 1765287791,     // Issued At      │
│    "exp": 1765288691      // Expiration     │
│  }                                          │
│                                             │
├─────────────────────────────────────────────┤
│                                             │
│  Signature (Verify Authenticity)            │
│  HMACSHA256(                                │
│    base64UrlEncode(header) + "." +          │
│    base64UrlEncode(payload),                │
│    JWT_SECRET                               │
│  )                                          │
│                                             │
└─────────────────────────────────────────────┘
```

---

## 🛡️ Authorization Flow (RBAC)

### Role-Based Access Control Architecture

```
┌─────────────────────────────────────────────┐
│              Role Hierarchy                  │
├─────────────────────────────────────────────┤
│                                             │
│  ┌─────────────┐                           │
│  │    ADMIN    │  (Highest Privilege)       │
│  └──────┬──────┘                           │
│         │                                   │
│         │ Can do everything                 │
│         │                                   │
│  ┌──────▼──────┐                           │
│  │   AUTHOR    │  (Content Creator)         │
│  └──────┬──────┘                           │
│         │                                   │
│         │ Can create/edit own content       │
│         │                                   │
│  ┌──────▼──────┐                           │
│  │   READER    │  (Basic User)              │
│  └─────────────┘                           │
│         │                                   │
│         │ Read-only access                  │
│         │                                   │
└─────────────────────────────────────────────┘
```

### Authorization Flow Diagram

```
┌──────────────┐
│   Client     │
│  (Logged in) │
└──────┬───────┘
       │
       │ Request with JWT Token
       │ POST /posts (Create new post)
       ▼
┌─────────────────────────────────┐
│    JwtAuthGuard                 │
│  ✅ Verify token                │
│  ✅ Extract user payload        │
└──────────┬──────────────────────┘
           │
           │ req.user = { id: 2, email: "...", role: "READER" }
           ▼
┌─────────────────────────────────┐
│    RolesGuard                   │
│  1. Read @Roles() decorator     │
│  2. Required: ["ADMIN","AUTHOR"]│
│  3. User role: "READER"         │
└──────────┬──────────────────────┘
           │
           ├─── ❌ Role not allowed
           │    → 403 Forbidden
           │    { message: "Forbidden resource" }
           │
           └─── ✅ Role allowed
                │
                ▼
┌─────────────────────────────────┐
│    PostsController.create()     │
│  • Passed authorization         │
│  • Proceed to business logic    │
└──────────┬──────────────────────┘
           │
           ▼
┌─────────────────────────────────┐
│    PostsService.create()        │
│  • Create post                  │
│  • Set authorId from req.user   │
└──────────┬──────────────────────┘
           │
           │ Response: { post }
           ▼
┌──────────────┐
│   Client     │
└──────────────┘
```

### Role Permission Matrix

```
┌────────────────────┬──────┬────────┬────────┐
│   Operation        │ADMIN │ AUTHOR │ READER │
├────────────────────┼──────┼────────┼────────┤
│ View Posts         │  ✅  │   ✅   │   ✅   │
│ Create Post        │  ✅  │   ✅   │   ❌   │
│ Edit Own Post      │  ✅  │   ✅   │   ❌   │
│ Edit Any Post      │  ✅  │   ❌   │   ❌   │
│ Delete Own Post    │  ✅  │   ✅   │   ❌   │
│ Delete Any Post    │  ✅  │   ❌   │   ❌   │
├────────────────────┼──────┼────────┼────────┤
│ Create Category    │  ✅  │   ❌   │   ❌   │
│ Edit Category      │  ✅  │   ❌   │   ❌   │
│ Delete Category    │  ✅  │   ❌   │   ❌   │
├────────────────────┼──────┼────────┼────────┤
│ Create Tag         │  ✅  │   ❌   │   ❌   │
│ Edit Tag           │  ✅  │   ❌   │   ❌   │
│ Delete Tag         │  ✅  │   ❌   │   ❌   │
├────────────────────┼──────┼────────┼────────┤
│ Comment on Post    │  ✅  │   ✅   │   ✅   │
│ Edit Own Comment   │  ✅  │   ✅   │   ✅   │
│ Delete Any Comment │  ✅  │   ❌   │   ❌   │
├────────────────────┼──────┼────────┼────────┤
│ Create User        │  ✅  │   ❌   │   ❌   │
│ Edit Own Profile   │  ✅  │   ✅   │   ✅   │
│ Edit Any Profile   │  ✅  │   ❌   │   ❌   │
└────────────────────┴──────┴────────┴────────┘
```

### Ownership Validation Flow

```
┌──────────────┐
│   Author     │
│  (ID: 2)     │
└──────┬───────┘
       │
       │ DELETE /posts/5
       │ Authorization: Bearer <token>
       ▼
┌─────────────────────────────────┐
│    RolesGuard                   │
│  ✅ User is AUTHOR or ADMIN     │
└──────────┬──────────────────────┘
           │
           ▼
┌─────────────────────────────────┐
│    PostsService.remove()        │
│  1. Find post by ID             │
│  2. Check post.authorId == 2    │
└──────────┬──────────────────────┘
           │
           ├─── ❌ Not owner (authorId = 3)
           │    → 403 Forbidden
           │    { message: "You can only delete your own posts" }
           │
           └─── ✅ Is owner (authorId = 2)
                │
                ▼
┌─────────────────────────────────┐
│    Delete Post                  │
│  • CASCADE delete comments      │
│  • CASCADE delete post tags     │
└──────────┬──────────────────────┘
           │
           │ Response: { message: "Post deleted" }
           ▼
┌──────────────┐
│   Client     │
└──────────────┘
```

### Guard Execution Order

```
Request
  │
  ▼
┌─────────────────────────────────┐
│  1. JwtAuthGuard                │
│     • Checks if user logged in  │
│     • Verifies JWT token        │
│     • Sets req.user             │
└──────────┬──────────────────────┘
           │
           │ If @Public() → Skip
           │
           ▼
┌─────────────────────────────────┐
│  2. RolesGuard                  │
│     • Reads @Roles() metadata   │
│     • Checks user.role          │
│     • Allow/Deny access         │
└──────────┬──────────────────────┘
           │
           ▼
┌─────────────────────────────────┐
│  3. Controller Method           │
│     • Business logic            │
└──────────┬──────────────────────┘
           │
           ▼
┌─────────────────────────────────┐
│  4. Service Layer               │
│     • Ownership validation      │
│     • Database operations       │
└─────────────────────────────────┘
```

---

## 🔄 API Request Flow

### Complete Request Lifecycle

```
┌─────────────────────────────────────────────────────────┐
│                    Client Request                        │
│  POST /api/v1/posts                                     │
│  Headers:                                               │
│    - Authorization: Bearer <JWT>                        │
│    - Content-Type: application/json                     │
│  Body:                                                  │
│    { "title": "...", "content": "...", "categoryId": 1 }│
└────────────────────────┬────────────────────────────────┘
                         │
                         ▼
         ┌───────────────────────────────┐
         │   Step 1: Middleware Layer    │
         └───────────┬───────────────────┘
                     │
    ┌────────────────┼────────────────┐
    │                │                │
    ▼                ▼                ▼
┌────────┐    ┌──────────┐    ┌──────────┐
│ Helmet │    │   CORS   │    │Throttler │
│Headers │    │  Check   │    │ Limit    │
└────────┘    └──────────┘    └──────────┘
    │                │                │
    └────────────────┼────────────────┘
                     │
                     ▼
         ┌───────────────────────────────┐
         │   Step 2: Guard Layer         │
         └───────────┬───────────────────┘
                     │
    ┌────────────────┼────────────────┐
    │                │                │
    ▼                ▼                │
┌──────────────┐ ┌──────────────┐    │
│JwtAuthGuard  │ │ RolesGuard   │    │
│• Verify JWT  │ │• Check role  │    │
│• Set req.user│ │• Allow/Deny  │    │
└──────────────┘ └──────────────┘    │
    │                │                │
    │ ✅ Valid       │ ✅ Authorized  │
    └────────────────┼────────────────┘
                     │
                     ▼
         ┌───────────────────────────────┐
         │   Step 3: Validation Layer    │
         └───────────┬───────────────────┘
                     │
                     ▼
         ┌───────────────────────────────┐
         │    ValidationPipe             │
         │  • Validate DTO               │
         │  • Transform data             │
         │  • Check @IsNotEmpty, etc     │
         └───────────┬───────────────────┘
                     │
                     │ ✅ Valid data
                     ▼
         ┌───────────────────────────────┐
         │   Step 4: Controller Layer    │
         └───────────┬───────────────────┘
                     │
                     ▼
         ┌───────────────────────────────┐
         │    PostsController.create()   │
         │  • Extract user from request  │
         │  • Call service method        │
         └───────────┬───────────────────┘
                     │
                     ▼
         ┌───────────────────────────────┐
         │   Step 5: Service Layer       │
         └───────────┬───────────────────┘
                     │
                     ▼
         ┌───────────────────────────────┐
         │    PostsService.create()      │
         │  • Business logic             │
         │  • Validate ownership         │
         │  • Call Prisma                │
         └───────────┬───────────────────┘
                     │
                     ▼
         ┌───────────────────────────────┐
         │   Step 6: Data Access Layer   │
         └───────────┬───────────────────┘
                     │
                     ▼
         ┌───────────────────────────────┐
         │    Prisma Client              │
         │  • Build SQL query            │
         │  • Execute query              │
         │  • Return result              │
         └───────────┬───────────────────┘
                     │
                     ▼
         ┌───────────────────────────────┐
         │       MySQL Database          │
         │  • Insert record              │
         │  • Return created record      │
         └───────────┬───────────────────┘
                     │
                     ▼
         ┌───────────────────────────────┐
         │   Response Flow (Reverse)     │
         └───────────┬───────────────────┘
                     │
    Database → Prisma → Service → Controller
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│                    Client Response                       │
│  Status: 201 Created                                    │
│  Body:                                                  │
│  {                                                      │
│    "id": 7,                                             │
│    "title": "...",                                      │
│    "slug": "...",                                       │
│    "content": "...",                                    │
│    "published": false,                                  │
│    "authorId": 2,                                       │
│    "categoryId": 1,                                     │
│    "createdAt": "2025-12-09T...",                       │
│    "author": { ... },                                   │
│    "category": { ... }                                  │
│  }                                                      │
└─────────────────────────────────────────────────────────┘
```

---

## 📝 CRUD Operations Flow

### Create Post Flow (with Tags)

```
Client Request:
POST /api/v1/posts
{
  "title": "Understanding Prisma Relations",
  "content": "...",
  "categoryId": 1,
  "published": true
}
  │
  ▼
┌─────────────────────────────────┐
│ PostsController.create()        │
│ • @Roles(['ADMIN', 'AUTHOR'])   │
└──────────┬──────────────────────┘
           │
           ▼
┌─────────────────────────────────┐
│ PostsService.create()           │
│ 1. Generate slug from title     │
│ 2. Set authorId from req.user   │
│ 3. Create post in database      │
└──────────┬──────────────────────┘
           │
           ▼
┌─────────────────────────────────┐
│ Prisma Query                    │
│ prisma.post.create({            │
│   data: {                       │
│     title,                      │
│     slug,                       │
│     content,                    │
│     authorId,                   │
│     categoryId,                 │
│     published                   │
│   },                            │
│   include: {                    │
│     author: true,               │
│     category: true              │
│   }                             │
│ })                              │
└──────────┬──────────────────────┘
           │
           ▼
Response: Created Post with Relations
```

### Read Post with All Relations

```
Client Request:
GET /api/v1/posts/1
  │
  ▼
┌─────────────────────────────────┐
│ PostsController.findOne(1)      │
│ • @Public() - No auth required  │
└──────────┬──────────────────────┘
           │
           ▼
┌─────────────────────────────────┐
│ PostsService.findOne(1)         │
│ 1. Find post by ID              │
│ 2. Include all relations        │
│ 3. Increment view count         │
└──────────┬──────────────────────┘
           │
           ▼
┌─────────────────────────────────┐
│ Prisma Query                    │
│ prisma.post.findUnique({        │
│   where: { id: 1 },             │
│   include: {                    │
│     author: {                   │
│       select: { id, name, email }│
│     },                          │
│     category: true,             │
│     tags: {                     │
│       include: {                │
│         tag: true               │
│       }                         │
│     },                          │
│     comments: {                 │
│       include: {                │
│         author: true            │
│       }                         │
│     },                          │
│     _count: {                   │
│       select: { comments: true }│
│     }                           │
│   }                             │
│ })                              │
└──────────┬──────────────────────┘
           │
           ▼
Response: Complete Post Data
{
  "id": 1,
  "title": "...",
  "author": { "id": 2, "name": "John" },
  "category": { "name": "Web Dev" },
  "tags": [
    { "tag": { "name": "NestJS" } },
    { "tag": { "name": "Prisma" } }
  ],
  "comments": [...],
  "_count": { "comments": 3 }
}
```

### Update with Ownership Check

```
Client Request:
PATCH /api/v1/posts/5
Authorization: Bearer <token>
{
  "title": "Updated Title",
  "published": true
}
  │
  ▼
┌─────────────────────────────────┐
│ Guards                          │
│ • JwtAuthGuard ✅               │
│ • RolesGuard (ADMIN/AUTHOR) ✅  │
└──────────┬──────────────────────┘
           │
           ▼
┌─────────────────────────────────┐
│ PostsService.update(5, data)    │
│ 1. Find post                    │
│ 2. Check ownership              │
└──────────┬──────────────────────┘
           │
           ▼
┌─────────────────────────────────┐
│ Ownership Validation            │
│ if (post.authorId !== userId    │
│     && role !== 'ADMIN') {      │
│   throw ForbiddenException      │
│ }                               │
└──────────┬──────────────────────┘
           │
           │ ✅ Authorized
           ▼
┌─────────────────────────────────┐
│ Prisma Update                   │
│ prisma.post.update({            │
│   where: { id: 5 },             │
│   data: updateDto               │
│ })                              │
└──────────┬──────────────────────┘
           │
           ▼
Response: Updated Post
```

### Delete with CASCADE

```
Client Request:
DELETE /api/v1/posts/5
  │
  ▼
┌─────────────────────────────────┐
│ PostsService.remove(5)          │
│ • Check ownership               │
└──────────┬──────────────────────┘
           │
           ▼
┌─────────────────────────────────┐
│ Prisma Delete                   │
│ prisma.post.delete({            │
│   where: { id: 5 }              │
│ })                              │
└──────────┬──────────────────────┘
           │
           ▼
┌─────────────────────────────────┐
│ Database CASCADE Operations     │
│ 1. Delete post (id: 5)          │
│ 2. Delete comments (postId: 5)  │
│ 3. Delete post_tags (postId: 5) │
└──────────┬──────────────────────┘
           │
           ▼
Response: { message: "Post deleted" }
```

### Many-to-Many: Add Tag to Post

```
Client Request:
POST /api/v1/posts/3/tags/2
  │
  ▼
┌─────────────────────────────────┐
│ PostsService.addTag(3, 2)       │
│ • Validate post ownership       │
│ • Check if tag exists           │
└──────────┬──────────────────────┘
           │
           ▼
┌─────────────────────────────────┐
│ Create Join Table Record        │
│ prisma.postTag.create({         │
│   data: {                       │
│     postId: 3,                  │
│     tagId: 2                    │
│   }                             │
│ })                              │
└──────────┬──────────────────────┘
           │
           ▼
┌─────────────────────────────────┐
│ Database Insert                 │
│ INSERT INTO PostTag             │
│   (postId, tagId)               │
│ VALUES (3, 2)                   │
└──────────┬──────────────────────┘
           │
           ▼
Response: { message: "Tag added to post" }
```

---

## 🔒 Security Layers

### Security Stack

```
┌─────────────────────────────────────────────┐
│          Layer 1: Network Security          │
│  • CORS (Allowed origins)                   │
│  • Rate Limiting (100 req/min global)       │
│  • Helmet (Security headers)                │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│       Layer 2: Authentication               │
│  • JWT tokens (signed & verified)           │
│  • Password hashing (bcrypt)                │
│  • Refresh token rotation                   │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│       Layer 3: Authorization                │
│  • Role-Based Access Control                │
│  • Resource ownership validation            │
│  • Guard-based permission checks            │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│       Layer 4: Input Validation             │
│  • DTO validation (class-validator)         │
│  • Type checking (TypeScript)               │
│  • Whitelist & transform                    │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│       Layer 5: Data Access                  │
│  • Prisma type-safe queries                 │
│  • SQL injection prevention                 │
│  • Parameterized queries                    │
└─────────────────────────────────────────────┘
```

### Password Security Flow

```
┌─────────────────────────────────┐
│  User Registration              │
│  Password: "password123"        │
└──────────┬──────────────────────┘
           │
           ▼
┌─────────────────────────────────┐
│  bcrypt.hash(password, 10)      │
│  • Generate salt (10 rounds)    │
│  • Hash password                │
└──────────┬──────────────────────┘
           │
           ▼
┌─────────────────────────────────┐
│  Store Hashed Password          │
│  "$2b$10$abcd...xyz"           │
└──────────┬──────────────────────┘
           │
           ▼
┌─────────────────────────────────┐
│  Login Attempt                  │
│  Input: "password123"           │
└──────────┬──────────────────────┘
           │
           ▼
┌─────────────────────────────────┐
│  bcrypt.compare(input, stored)  │
│  • Hash input with same salt    │
│  • Compare hashes               │
└──────────┬──────────────────────┘
           │
           ├─── ❌ Not match
           │    → Unauthorized
           │
           └─── ✅ Match
                → Generate JWT
```

### Rate Limiting Flow

```
┌──────────────┐
│   Client     │
└──────┬───────┘
       │
       │ Request 1 ──► ✅ Allowed
       │ Request 2 ──► ✅ Allowed
       │ Request 3 ──► ✅ Allowed
       │ ...
       │ Request 100 ─► ✅ Allowed
       │ Request 101 ─► ❌ BLOCKED
       ▼
┌─────────────────────────────────┐
│  Throttler Interceptor          │
│  • Track requests per IP        │
│  • Window: 60 seconds           │
│  • Limit: 100 requests          │
└──────────┬──────────────────────┘
           │
           │ 429 Too Many Requests
           ▼
┌──────────────┐
│   Client     │
│  (Wait 60s)  │
└──────────────┘

Special Limits:
• /auth/login:    5 requests/min
• /auth/register: 3 requests/min
```

### Security Headers (Helmet)

```
Response Headers:
┌─────────────────────────────────────────────┐
│ X-Content-Type-Options: nosniff             │
│  → Prevent MIME-type sniffing               │
│                                             │
│ X-Frame-Options: DENY                       │
│  → Prevent clickjacking attacks             │
│                                             │
│ X-XSS-Protection: 1; mode=block             │
│  → Enable XSS filter                        │
│                                             │
│ Strict-Transport-Security: max-age=...      │
│  → Force HTTPS connections                  │
│                                             │
│ Content-Security-Policy: default-src 'self' │
│  → Restrict resource loading                │
└─────────────────────────────────────────────┘
```

---

## 🚀 Complete User Journey

### Journey 1: New User Registration to Publishing Post

```
Step 1: Registration
┌──────────────┐
│   Visitor    │
└──────┬───────┘
       │
       │ POST /auth/register
       │ { email, password, name }
       ▼
✅ Account Created
   • User record in DB
   • Profile created (1:1)
   • JWT tokens issued
   • Refresh token stored

Step 2: Login
┌──────────────┐
│   User       │
└──────┬───────┘
       │
       │ POST /auth/login
       │ { email, password }
       ▼
✅ Authenticated
   • Access token (15min)
   • Refresh token (7days)

Step 3: View Profile
┌──────────────┐
│   User       │
└──────┬───────┘
       │
       │ GET /auth/profile
       │ Authorization: Bearer <token>
       ▼
✅ Profile Data
   • User info
   • Profile details (1:1 relation)
   • Post count

Step 4: Update Profile
┌──────────────┐
│   User       │
└──────┬───────┘
       │
       │ PATCH /profiles/:userId
       │ { bio, avatar, website }
       ▼
✅ Profile Updated

Step 5: Create Post
┌──────────────┐
│   Author     │
└──────┬───────┘
       │
       │ POST /posts
       │ { title, content, categoryId }
       ▼
✅ Post Created
   • Slug auto-generated
   • Author linked (M:1)
   • Category linked (M:1)

Step 6: Add Tags to Post
┌──────────────┐
│   Author     │
└──────┬───────┘
       │
       │ POST /posts/:postId/tags/:tagId
       ▼
✅ Tag Added
   • PostTag record created (M:N)

Step 7: Publish Post
┌──────────────┐
│   Author     │
└──────┬───────┘
       │
       │ PATCH /posts/:id
       │ { published: true }
       ▼
✅ Post Published
   • Visible to public

Step 8: Readers Comment
┌──────────────┐
│   Reader     │
└──────┬───────┘
       │
       │ POST /posts/:postId/comments
       │ { content }
       ▼
✅ Comment Added
   • Comment linked to post (M:1)
   • Comment linked to author (M:1)

Step 9: View Post with All Relations
┌──────────────┐
│   Anyone     │
└──────┬───────┘
       │
       │ GET /posts/:id
       ▼
✅ Complete Post Data
   • Post details
   • Author info (M:1)
   • Category (M:1)
   • Tags array (M:N)
   • Comments array (1:N)
   • Comment count
```

### Journey 2: Admin Managing Content

```
Step 1: Admin Login
┌──────────────┐
│   Admin      │
└──────┬───────┘
       │
       │ POST /auth/login
       │ { email: "admin@blog.com" }
       ▼
✅ Admin Access

Step 2: Create Category
┌──────────────┐
│   Admin      │
└──────┬───────┘
       │
       │ POST /categories
       │ { name, description }
       ▼
✅ Category Created
   • Slug auto-generated
   • Admin only operation

Step 3: Create Tags
┌──────────────┐
│   Admin      │
└──────┬───────┘
       │
       │ POST /tags
       │ { name }
       ▼
✅ Tag Created
   • Available for all posts

Step 4: Create User
┌──────────────┐
│   Admin      │
└──────┬───────┘
       │
       │ POST /users
       │ { email, password, name, role }
       ▼
✅ New User Created
   • Admin only operation
   • Can set any role

Step 5: Moderate Content
┌──────────────┐
│   Admin      │
└──────┬───────┘
       │
       │ DELETE /posts/:id
       │ (Any post, not just own)
       ▼
✅ Post Deleted
   • CASCADE: Comments deleted
   • CASCADE: PostTags deleted

Step 6: Delete Comment
┌──────────────┐
│   Admin      │
└──────┬───────┘
       │
       │ DELETE /comments/:id
       │ (Any comment)
       ▼
✅ Comment Removed
   • Admin can delete any comment
```

---

## 📦 Deployment Flow

### From Development to Production

```
┌─────────────────────────────────────────────┐
│         Phase 1: Development                 │
└──────────────────┬──────────────────────────┘
                   │
    ┌──────────────┼──────────────┐
    │              │              │
    ▼              ▼              ▼
┌────────┐  ┌──────────┐  ┌──────────┐
│  Code  │  │ Database │  │  Test    │
│ Source │  │  Local   │  │ Postman  │
└────────┘  └──────────┘  └──────────┘
    │              │              │
    └──────────────┼──────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│         Phase 2: Version Control             │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
         ┌──────────────────┐
         │   Git Commit     │
         │   • Add files    │
         │   • Commit       │
         │   • Push         │
         └─────────┬────────┘
                   │
                   ▼
         ┌──────────────────┐
         │   GitHub Repo    │
         └─────────┬────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│         Phase 3: CI/CD Pipeline              │
└──────────────────┬──────────────────────────┘
                   │
    ┌──────────────┼──────────────┐
    │              │              │
    ▼              ▼              ▼
┌────────┐  ┌──────────┐  ┌──────────┐
│ Build  │  │   Test   │  │  Deploy  │
│        │  │  Suite   │  │          │
└────────┘  └──────────┘  └──────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│         Phase 4: Production Setup            │
└──────────────────┬──────────────────────────┘
                   │
    ┌──────────────┼──────────────┐
    │              │              │
    ▼              ▼              ▼
┌────────┐  ┌──────────┐  ┌──────────┐
│Database│  │   App    │  │  Nginx   │
│ MySQL  │  │ Server   │  │ Reverse  │
│        │  │ NestJS   │  │  Proxy   │
└────────┘  └──────────┘  └──────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│         Phase 5: Monitoring                  │
└──────────────────┬──────────────────────────┘
                   │
    ┌──────────────┼──────────────┐
    │              │              │
    ▼              ▼              ▼
┌────────┐  ┌──────────┐  ┌──────────┐
│  Logs  │  │Metrics   │  │  Alerts  │
└────────┘  └──────────┘  └──────────┘
```

### Environment Configuration

```
Development:
┌─────────────────────────────────┐
│ DATABASE_URL=mysql://localhost  │
│ JWT_SECRET=dev-secret           │
│ NODE_ENV=development            │
│ PORT=3000                       │
└─────────────────────────────────┘

Production:
┌─────────────────────────────────┐
│ DATABASE_URL=mysql://prod-host  │
│ JWT_SECRET=<strong-secret>      │
│ NODE_ENV=production             │
│ PORT=3000                       │
│ CORS_ORIGIN=https://app.com     │
└─────────────────────────────────┘
```

### Database Migration Flow

```
Development:
┌──────────────┐
│  1. Change   │
│    Schema    │
└──────┬───────┘
       │
       ▼
┌──────────────┐
│  2. Generate │
│   Migration  │
│ prisma migrate│
│     dev      │
└──────┬───────┘
       │
       ▼
┌──────────────┐
│  3. Test     │
│  Locally     │
└──────┬───────┘
       │
       ▼
┌──────────────┐
│  4. Commit   │
│  Migration   │
│    Files     │
└──────────────┘

Production:
┌──────────────┐
│  1. Deploy   │
│     Code     │
└──────┬───────┘
       │
       ▼
┌──────────────┐
│  2. Run      │
│  Migration   │
│ prisma migrate│
│    deploy    │
└──────┬───────┘
       │
       ▼
┌──────────────┐
│  3. Verify   │
│   Schema     │
└──────────────┘
```

---

## 🎓 Summary

### Key Concepts Demonstrated

1. **Database Relations**
   - One-to-One: User ↔ Profile
   - One-to-Many: User → Posts, Post → Comments
   - Many-to-Many: Posts ↔ Tags (explicit join table)

2. **Authentication**
   - JWT-based stateless authentication
   - Access tokens (short-lived)
   - Refresh tokens (long-lived)
   - Password hashing with bcrypt

3. **Authorization**
   - Role-Based Access Control (RBAC)
   - Guard-based permission checks
   - Resource ownership validation
   - Decorator-based metadata

4. **Security**
   - Helmet security headers
   - Rate limiting
   - CORS configuration
   - Input validation
   - SQL injection prevention

5. **Best Practices**
   - Separation of concerns
   - Type safety with TypeScript
   - DTO validation
   - Error handling
   - Clean architecture

### API Endpoints Summary

```
Authentication:
POST   /api/v1/auth/register
POST   /api/v1/auth/login
POST   /api/v1/auth/refresh
POST   /api/v1/auth/logout
GET    /api/v1/auth/profile

Users:
GET    /api/v1/users
GET    /api/v1/users/:id
POST   /api/v1/users          [ADMIN]
PATCH  /api/v1/users/:id      [ADMIN, OWNER]
DELETE /api/v1/users/:id      [ADMIN, OWNER]

Posts:
GET    /api/v1/posts
GET    /api/v1/posts/:id
GET    /api/v1/posts/slug/:slug
POST   /api/v1/posts          [ADMIN, AUTHOR]
PATCH  /api/v1/posts/:id      [ADMIN, OWNER]
DELETE /api/v1/posts/:id      [ADMIN, OWNER]
POST   /api/v1/posts/:postId/tags/:tagId     [ADMIN, OWNER]
DELETE /api/v1/posts/:postId/tags/:tagId     [ADMIN, OWNER]

Categories:
GET    /api/v1/categories
GET    /api/v1/categories/:id
POST   /api/v1/categories     [ADMIN]
PATCH  /api/v1/categories/:id [ADMIN]
DELETE /api/v1/categories/:id [ADMIN]

Comments:
GET    /api/v1/posts/:postId/comments
POST   /api/v1/posts/:postId/comments
PATCH  /api/v1/comments/:id   [ADMIN, OWNER]
DELETE /api/v1/comments/:id   [ADMIN, OWNER]

Tags:
GET    /api/v1/tags
GET    /api/v1/tags/:id
POST   /api/v1/tags           [ADMIN]
PATCH  /api/v1/tags/:id       [ADMIN]
DELETE /api/v1/tags/:id       [ADMIN]

Profiles:
GET    /api/v1/profiles/user/:userId
POST   /api/v1/profiles
PATCH  /api/v1/profiles/:userId    [ADMIN, OWNER]
DELETE /api/v1/profiles/:userId    [ADMIN, OWNER]
```

---

## 📚 Additional Resources

- **NestJS Documentation**: https://docs.nestjs.com
- **Prisma Documentation**: https://www.prisma.io/docs
- **JWT Introduction**: https://jwt.io/introduction
- **RBAC Best Practices**: https://auth0.com/docs/manage-users/access-control/rbac
- **API Security**: https://owasp.org/www-project-api-security

---

**Created for Week 24 - RevoU Full Stack Software Engineering**
